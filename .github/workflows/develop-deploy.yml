name: Deploy to staging

on:
  push:
    branches: [ "release" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging-env
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to staging
        run: echo "staging to ${{ vars.SERVER_URL }}"

